<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:ctx="http://www.springframework.org/schema/context"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
">

    <!-- для @PostConstruct (а так же включает обработчики других спринговых аннотаций) -->
    <ctx:annotation-config/>
    <!-- Указываем где искать компоненты (репозитории и сервисы) -->
    <ctx:component-scan base-package="com.train.repository, com.train.service"/>
    <!-- Спринг умеет использовать подстановку параметров, указываем где брать значения -->
    <ctx:property-placeholder location="classpath:application.properties"/>

    <!-- Настройка источника данных, может быть для любой другой базы,
         параметры укаываются в application.properties и подставляются ниже благодаря property-placeholder -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${datasource.driver}"/>
        <property name="url" value="${datasource.url}"/>
        <property name="username" value="${datasource.username}"/>
        <property name="password" value="${datasource.password}"/>
    </bean>

    <!-- Бин, непосредственно который мы и будем использовать для работы с базой -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource"/>
    </bean>

    <!-- Бин, который накатит нам базу при старте приложения -->
    <bean id="liquibase" class="liquibase.integration.spring.SpringLiquibase">
        <property name="changeLog" value="classpath:database-changelog.xml"/>
        <property name="dataSource" ref="dataSource"/>
    </bean>
</beans>
